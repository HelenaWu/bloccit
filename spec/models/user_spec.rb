require 'rails_helper'

describe User do 
  include TestFactories
  
  describe "#favourited(post)" do
    xit "returns `nil` if the user has not favourited the post" do
    end

    xit "returns the appropriate favourite if it exists" do
    end
    
  end
end